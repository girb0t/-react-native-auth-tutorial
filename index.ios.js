import { AppRegistry } from 'react-native';
import App from './src/app';

// the first argument ('auth') has to match the name that was used to generate
// this project
AppRegistry.registerComponent('auth', () => App);
